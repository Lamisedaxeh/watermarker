{
  "name": "lamiwatermarker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/main.js -o build/app.js && cp src/index.html build/ && mkdir -p build/assets && cp src/assets/* build/assets"
  },
  "author": "Lamisedaxeh",
  "license": "",
  "dependencies": {
    "blob-stream": "^0.1.3",
    "brfs": "^2.0.2",
    "browserify": "^17.0.0",
    "iconv-lite": "^0.6.3",
    "pdfkit": "^0.13.0"
  }
}
